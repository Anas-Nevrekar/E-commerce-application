<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Buy Cart Products</title>
    <style>
        body { background: #f6f8fa; font-family: 'Segoe UI', Arial, sans-serif; margin: 0; padding: 0; }
        .container { max-width: 600px; margin: 40px auto; background: #fff; border-radius: 16px; box-shadow: 0 8px 32px rgba(60,60,60,0.12); padding: 32px 24px; }
        h1 { text-align: center; color: #222; }
        .product-list { margin: 24px 0; }
        .product-item { display: flex; align-items: center; margin-bottom: 18px; border-bottom: 1px solid #eee; padding-bottom: 12px; }
        .product-img { width: 64px; height: 64px; object-fit: cover; border-radius: 8px; margin-right: 18px; }
        .product-info { flex: 1; }
        .product-title { font-size: 1.1rem; font-weight: 500; }
        .product-price { color: #0a7d2c; font-weight: bold; }
        .total { text-align: right; font-size: 1.2rem; margin-top: 18px; font-weight: bold; }
        .buy-btn { background: linear-gradient(90deg, #ff9800 0%, #ff5722 100%); color: #fff; border: none; border-radius: 8px; padding: 14px 36px; font-size: 1.1rem; font-weight: 600; cursor: pointer; transition: background 0.2s, box-shadow 0.2s; box-shadow: 0 2px 8px rgba(255,152,0,0.08);}
        .buy-btn:hover { background: linear-gradient(90deg, #ff5722 0%, #ff9800 100%);}
        .back-link { display: inline-block; margin-top: 24px; color: #1976d2; text-decoration: none; font-size: 1rem; transition: color 0.2s;}
        .back-link:hover { color: #0d47a1; text-decoration: underline;}
    </style>
</head>
<body>
    <div class="container">
        <h1>Buy Cart Products</h1>
        <% if (cartProducts.length === 0) { %>
            <p>Your cart is empty.</p>
        <% } else { %>
            <div class="product-list">
                <% cartProducts.forEach(product => { %>
                    <div class="product-item">
                        <img class="product-img" src="<%= product.productImage %>" alt="">
                        <div class="product-info">
                            <div class="product-title"><%= product.productName %></div>
                            <div class="product-price">$<%= product.productPrice %></div>
                        </div>
                    </div>
                <% }) %>
            </div>
            <div class="total">Total: $<%= total %></div>
            <form action="/home/buyCart" method="post" style="text-align:center; margin-top: 24px;">
                <button class="buy-btn" type="submit">Buy All</button>
            </form>
        <% } %>
        <a class="back-link" href="/home/profile">&larr; Back to Profile</a>
    </div>
</body>
</html>