<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
  body {
    font-family: 'Segoe UI', Arial, sans-serif;
    background: #f4f6f9;
    margin: 0;
    padding: 20px;
  }
  h1, h2 {
    text-align: center;
    color: #222;
  }
  p {
    font-size: 1rem;
    color: #444;
  }
  form {
    margin: 16px auto;
    max-width: 420px;
    background: #fff;
    padding: 20px;
    border-radius: 12px;
    box-shadow: 0 6px 20px rgba(0,0,0,0.08);
  }
  label {
    font-weight: 500;
    display: block;
    margin: 10px 0 6px;
  }
  input {
    width: 100%;
    padding: 10px;
    border-radius: 8px;
    border: 1px solid #ccc;
    margin-bottom: 12px;
  }
  button {
    background: #1976d2;
    border: none;
    padding: 10px 20px;
    border-radius: 8px;
    color: #fff;
    font-weight: bold;
    cursor: pointer;
    transition: background 0.2s;
  }
  button:hover {
    background: #0d47a1;
  }
  .product-list {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: 20px;
    margin-top: 20px;
  }
  .product-item {
    background: #fff;
    border-radius: 12px;
    padding: 20px;
    text-align: center;
    box-shadow: 0 6px 20px rgba(0,0,0,0.08);
  }
  .product-item h2 {
    margin: 10px 0;
  }
</style>

</head>
<body>
    <h1>This is profile page</h1>
    <p>Username: <%= user.username %></p>
    <p>Email: <%= user.email %></p>

        <form action ="/home/updateProfile" method="post">
       
        <label for="address">Address:</label>
        <input type="text" id="address" name="address" value="<%= user.address %>" required>
        <br><br>
        <button type="submit">Update Profile</button>
        </form>
        <br>
        <form action="/home" method="get">
            <button type="submit">homepage </button>
         </form>
    <h2>Cart Products</h2>
    <% if (cartProducts.length === 0) { %>
      <p>No products in your cart.</p>
    <% } else { %>
      <div class="product-list">
        <% cartProducts.forEach(product => { %>
          <div class="product-item" style="margin-bottom: 24px; padding: 12px; border: 1px solid #eee; border-radius: 8px;">
            <img
              src="<%= product.productImage %>"
              alt="Product image"
              style="
                width: 96px;
                height: 96px;
                object-fit: cover;
                border-radius: 50%;
                border: 2px solid #fff;
                box-shadow: 0 4px 14px rgba(0,0,0,0.18);
                background: #f3f3f3;
                display: block;
              "
              loading="lazy"
              decoding="async"
            />
            <h2><%= product.productName %> - $<%= product.productPrice %></h2>
            <p><%= product.productDescription %></p>
            <form action="/home/removeFromCart/<%= product._id %>" method="post">
              <button type="submit">Remove from Cart</button>
            </form>
          </div>
        <% }); %>
      </div>
    <% } %>
</body>
</html>


