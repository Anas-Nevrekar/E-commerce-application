<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Buy <%= product.productName %></title>
    <style>
        body {
            background: #f6f8fa;
            font-family: 'Segoe UI', Arial, sans-serif;
            margin: 0;
            padding: 0;
        }
        .buy-container {
            max-width: 500px;
            margin: 40px auto;
            background: #fff;
            border-radius: 16px;
            box-shadow: 0 8px 32px rgba(60,60,60,0.12);
            padding: 32px 24px;
            text-align: center;
        }
        .buy-container h1 {
            color: #222;
            margin-bottom: 16px;
            font-size: 2rem;
        }
        .product-img {
            width: 320px;
            height: 320px;
            object-fit: cover;
            border-radius: 12px;
            box-shadow: 0 4px 18px rgba(0,0,0,0.13);
            margin-bottom: 18px;
        }
        .product-title {
            font-size: 1.5rem;
            color: #1a1a1a;
            margin: 10px 0 8px 0;
        }
        .product-desc {
            color: #555;
            font-size: 1rem;
            margin-bottom: 18px;
        }
        .product-price {
            font-size: 1.2rem;
            color: #0a7d2c;
            font-weight: bold;
            margin-bottom: 24px;
        }
        .buy-btn {
            background: linear-gradient(90deg, #ff9800 0%, #ff5722 100%);
            color: #fff;
            border: none;
            border-radius: 8px;
            padding: 14px 36px;
            font-size: 1.1rem;
            font-weight: 600;
            cursor: pointer;
            transition: background 0.2s, box-shadow 0.2s;
            box-shadow: 0 2px 8px rgba(255,152,0,0.08);
        }
        .buy-btn:hover {
            background: linear-gradient(90deg, #ff5722 0%, #ff9800 100%);
            box-shadow: 0 4px 16px rgba(255,87,34,0.13);
        }
        .back-link {
            display: inline-block;
            margin-top: 24px;
            color: #1976d2;
            text-decoration: none;
            font-size: 1rem;
            transition: color 0.2s;
        }
        .back-link:hover {
            color: #0d47a1;
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="buy-container">
        <h1>Checkout</h1>
        <img class="product-img" src="<%= product.productImage %>" alt="Product image">
        <div class="product-title"><%= product.productName %></div>
        <div class="product-desc"><%= product.productDescription %></div>
        <div class="product-price">$<%= product.productPrice %></div>
        <form action="/home/buy/<%= product._id %>" method="post">
            <button class="buy-btn" type="submit">Confirm BUY</button>
        </form>
        <a class="back-link" href="/home">&larr; Back to Home</a>
    </div>
</body>
</html>